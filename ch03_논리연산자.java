public class ch03_논리연산자 {
    // 조건식을 연결할 때 사용하는 연산자
    // ||(or결합) 피연산자 중 어느 한 쪽이 ture 이면 true를 결과로 얻는다.
    // &&(and결합) 피연산자 양쪽 모두 ture 여야 true 결과로 얻는다.

    // 3-16 논리 부정 연산자 !
    // true 를 false로, false는 true 로 바꾼다.
    // x -> !x
    // ture- > false 가 됨.
    // false -> true 가 됨.
    // 그냥 !=not 이라 봐라 연산 방향이 바뀌는 거임.
    // boolean b = ture;
    // !!b -> !!true -> !false -> ture
    // 뒤에있는 ! 먼저 계산 되고 앞에 !가 연산 되는 것이다.
}
