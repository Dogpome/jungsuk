public class ch_Scanner {
    //화면으로부터 데이터를 입력받는 기능을 제공하는 클래스
    // 스캐너 클래스를 사용하려면?
    // import문 추가
    // impor java.utill.*l
    // 2. scanner 객체의 생성
    // scanner canner = new Scanner System.in; (화면으로부터 입력받을거니까 시스템인)
    // 3. scanner 객체를 사용
    // (1) int num = scanner.nextInt(); // 화면에서 입력받은 정수를 num에 저장
    // (2)String input = scanner.nextLine(); // 화면에서 입력받은 내용은 input에 저장
    // (3)int num = integer.parseInt(input); // 문자열(input)을 숫자로(num)로 변환
    // (2)(3) 과 (1)은 같은 문장임.


}